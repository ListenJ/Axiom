# AGENTS.md — Runtime 底层规则（强约束）

> 规则版本：v1.2（2026-09-09）。v1.1：新增规则 12；v1.2：新增总则与任务分级/完成证据。规则编号只增不插；修改本文件须遵守规则 2/3/5。
> 执行顺序：读总则 0.1-0.3 → 判定任务级别 → 冻结任务契约（0.4）→ 按对应规则施工 → 按 0.5 提交完成证据。

以下规则为本仓库所有任务的**强制约束**，任何 agent（含子 agent）执行任务时必须遵守。

## 总则：优先级、任务分级与完成证据

### 0.1 规则优先级（冲突时从高到低）

1. **安全与不可逆操作**：规则 9（Git 安全护栏）、规则 11（敏感资产）。
2. **数据完整与可追溯**：规则 2（备份/验证）、规则 3（提交/推送）、规则 5（操作留痕）。
3. **正确性与验证**：规则 6（调试）、规则 7（TDD）、规则 12 的 Check。
4. **范围与最小化**：规则 1、规则 12 的 Plan/Do、规则 8（设计边界）。
5. **风格与偏好**：周边代码风格、文档格式、个人习惯。

- 用户明确指令优先于第 3-5 级；第 1-2 级的例外仅按对应规则自身的例外条款处理（如规则 9 的例外），否则按规则 10 停止并请求决策。
- 同一优先级内冲突：先保证可验证、可回滚、可追溯，再选择改动更小的方案。
- 无法判断优先级时按更高风险处理，不得用“规则 1 最小改动”绕过安全、备份、留痕或验证。

### 0.2 规则编号与引用稳定

- 规则编号只追加，禁止插入、重排、复用；引用时写“规则 N + 标题”，不要只写编号。
- 规则计数与范围使用“以上全部规则”“除本规则外的其他规则”等自调整表述，禁止写死条数或区间。
- 修改本文件本身视为业务修改：必须遵守规则 2/3/5，并在提交信息中说明规则变更目的。
- 规则之间冲突时先按 0.1 排序；仍无法消解时停止施工，向用户说明冲突点与可选方案。

### 0.3 任务分级与施工门禁

| 级别 | 典型任务 | 必需门禁 |
| --- | --- | --- |
| T0 只读/咨询 | 代码勘察、方案评估、状态查询 | 不写文件；结论附证据；涉及外部信息按规则 10 |
| T1 小修/文档 | 单文件小修、文档、注释、配置值 | 任务契约（可内联）+ 验证命令 + 规则 5 留痕 |
| T2 功能/缺陷 | 多文件功能、bug 修复、测试补充 | T1 + 规则 6/7（按接缝）+ 回归测试 + 回滚点 |
| T3 架构/安全/运维 | 模块重构、接口变更、部署、凭据、数据迁移 | T2 + 书面计划 + 用户确认关键决策 + 回滚/迁移方案 + 影响面清单 |

- 分级由影响面决定，不由代码行数决定；不确定时按更高一级处理。
- 不允许用低级别规避必要门禁，也不允许用高级别给无关改动扩权。
- 子代理只执行被分配的子任务并遵守同一级别门禁；主 agent 负责集成、冲突消解与最终验证。

### 0.4 统一任务契约（Plan 的最小格式）

```
任务:
验收标准:
改动清单（文件级）:
不做项:
验证命令:
风险/回滚:
```

- T1 可内联在回复中；T2 必须在开工前明确；T3 必须落盘为计划文档。
- 计划未冻结不得动工；冻结后新增改动必须回 Plan 并说明原因。
- 每项改动须能回答“删掉它，验收标准是否仍成立”；若能，则不写。

### 0.5 统一完成证据（DoD）

- 实际 diff 与冻结清单一致；
- 验证命令、关键输出/退出码、未执行检查及原因；
- 规则 5 的 ops log 条目；
- commit hash 与推送目标；
- 偏差、未决项与后续项显式列出。

缺任一项不得声称完成；不得用“应该可以/大概通过”替代验证证据。

### 0.6 例外与停机条件

以下情况停止施工并请求用户决策，不得自行扩大范围或冒险执行：

- 触发规则 9 的破坏性操作；
- 涉及真实凭据、密钥、生产数据或外部系统写入；
- 任务范围、验收标准或规则之间冲突不清；
- 无法建立可重复验证回路，或验证需要无法获得的凭据/环境；
- 需要新增依赖、公共接口或跨模块改动而计划未覆盖。

任何规则例外必须在 ops log 的“偏差记录”中写明：规则、原因、风险、补偿措施。

### 0.7 规则变更协议

- 规则变更必须基于可观察问题（事故、返工、歧义、验证失败），不得凭偏好改写。
- 版本语义：主版本 = 不兼容变更（删除/重排规则、改变强制门禁）；次版本 = 新增规则或新增门禁；修订号 = 措辞澄清。
- 删除/重排规则编号、降低安全/备份/留痕门禁属于不兼容变更，必须经用户明确批准。
- 每次变更在 ops log 记录：问题证据、变更内容、影响规则、验证方式、回滚方案。
- 规则变更不得与无关业务改动混入同一提交；变更后必须运行结构校验（编号连续、引用无死链、无写死条数）。

## 规则 1：最小化施工，禁止过度施工

- 只做任务要求的最小改动；不顺手重构、不清理无关代码、不增加投机性的"通用能力"。
- 改动范围严格限定在任务涉及的文件与模块内。
- 新代码风格与周边代码保持一致。
- 施工过程按规则 12 的 PCDA 闭环执行（Plan → Do → Check → Act）。

## 规则 2：改代码前先备份 → 读全文 → 修改 → 验证 → 删备份

1. **备份**：修改任何**已存在**文件前，先将其复制到 `.tmp/backups/`（保留相对路径结构，如 `.tmp/backups/src/foo/bar.ts`）；新建文件无需备份（不存在旧版本），但必须纳入任务契约的改动清单。
2. **读全文**：修改前必须通读目标文件全文；超大文件（> 1MB 或 > 5000 行）可改为“读结构索引 + 修改点上下文 + 全仓引用”，但必须确认修改点与全局结构一致，禁止凭片段或记忆改动。
3. **修改**：按规则 1 做最小改动。
4. **验证**：运行覆盖该改动的检查（编译 / lint / 相关测试），记录实际命令、关键输出/退出码；无法运行时按总则 0.6 停机，不得口头声明通过。
5. **删除备份**：验证通过后，删除 `.tmp/backups/` 中对应备份；验证未通过或任务中断时保留备份，直到问题解决。
6. **多子 agent 协同**：任务可拆分时优先并行，但同一文件/模块禁止并行编辑；子代理只做被分配的子任务，不得扩大范围，各自备份、验证、记录；主 agent 负责集成、冲突消解与最终验证。
7. **敏感内容备份**：若待修改文件含真实密钥/凭据，备份不得放入 `.tmp/backups/`，应放到本地非仓库凭据目录，或验证后立即删除。

## 规则 3：所有修改提交到 git 仓库

- 目标仓库：remote 名 `internal211`，SSH 地址 `ssh://data@192.168.0.22/home/data/openclaw-fusion.git`（SSH 密钥已配置）。
- 禁止直接在 `main`/`master` 上提交业务修改；新分支默认 `codex/<任务简述>`，提交后推送 `internal211 <分支>`。
- 每个任务的修改完成后：`git add <仅本任务相关文件>` → `git commit` → `git push internal211 <当前分支>`。
- **只暂存本任务相关文件**，不得把工作区中无关的未提交改动一并提交；提交前用 `git diff --cached --stat` 核对暂存范围。
- 提交信息写清任务内容；规则文件变更须写明变更目的。
- 推送后用 `git status -sb` 或 `git ls-remote` 核对远端与本地一致；未推送成功不得声称完成。
- 已推送提交禁止 amend/rewrite；只能追加新提交，见规则 9。

## 规则 4：删除 = 新文件入仓库 + 旧文件归档

- "删除文件"操作的实质是**以新替旧**：修改后的新文件进入 git 仓库作为正式版本，旧文件按项目/模块归档。
- 归档落点遵循仓库实际策略：
  1. `archive/` 已在 `.gitignore` 中，仅作工作区本地归档参考，不进入常规提交。
  2. 若旧文件必须保留到 git 服务器，使用独立归档分支（如 `codex/archive-<日期>`），对该分支 `git add -f` 提交并推送，禁止并入 main/master。
- 具体执行流程：
  1. **新文件入仓库**：修改后的文件正常 `git add` → `git commit` → `git push`，成为仓库中的最新版本。
  2. **旧文件归档**：将被替换的旧文件移动到 `archive/` 下，按项目/模块分类存放（保持可识别的相对路径，如 `archive/<项目名>/<模块>/旧文件名`）。
  3. **归档记录**：在 `archive/ARCHIVE-LOG.md` 中追加一条记录：**归档时间、归档目的、原位置、归档位置、所属项目**。
  4. **工作区清理**：归档记录提交后，将旧文件从仓库工作区移除（`git rm`）并提交。
- 禁止未经归档记录直接删除任何文件；`git rm` 必须在归档记录提交之后方可执行。
- **例外（仅限无历史价值的生成物）**：未被 git 跟踪、可确定性重建、无代码/文档引用的缓存、构建产物、空目录，可直接删除；删除后必须在 ops log 说明路径与判定依据。已被 git 跟踪或有历史价值的文件不适用本例外。

## 规则 5：操作留痕 —— `docs/operations-log.md`

- 所有业务修改必须有可查询的痕迹：提交前在 `docs/operations-log.md` 追加一条记录，**提交一次记录一次**。
- 每条记录包含：时间、任务描述、使用的工具（含子代理）、执行的操作（文件级）、验证结果、对应 commit hash。
- 单条记录保持可检索：默认 ≤ 20 行；详细证据放 `docs/reports/` 并在条目中链接。主文件超过 2MB 或单月条目超过 200 条时，按月份拆分到 `docs/operations-log/YYYY-MM.md`，主文件保留索引与当月记录。
- hash 未知时先写占位符，主提交完成后立即回填；回填 hash 的提交属于记录维护，不再追加新的业务记录，避免无限递归。未推送提交可用 amend 回填；已推送提交只追加记录维护提交，禁止改写已发布历史（规则 9）。
- 对应任务使用对应工具，并如实记录工具与操作。

## 规则 6：调试纪律 —— 先建反馈回路，再提假设

> 来源：[mattpocock/skills · diagnosing-bugs](https://github.com/mattpocock/skills/blob/main/skills/engineering/diagnosing-bugs/SKILL.md)

调试硬 bug 时，禁止凭直觉读代码猜原因。必须先建立**紧反馈回路**（一条能复现该 bug 的命令），再进入假设阶段。

- **分级**：T1 级、单点/单文件、无并发与外部依赖的 trivial bug，可直接用现有测试或一条验证命令建立红→绿回路，跳过 Phase 2-4，但必须记录命令与结果；其余 bug 走完整 6 阶段。
1. **Phase 1 建反馈回路**（这是核心）：找到**一条命令**（测试 / curl / CLI / 最小 harness），它驱动真实 bug 路径并断言用户的**确切症状**，能红能绿（即修复前可复现失败、修复后可通过）。回路必须：可复现、确定性、秒级、agent 可独立运行；优先离线与夹具，需要无法获得的凭据/环境时按总则 0.6 停机。**无此命令不得进入 Phase 2。**
2. **Phase 2 复现 + 最小化**：确认回路变红，且症状与用户描述一致；逐步裁剪输入 / 调用 / 配置，直到剩余每个元素都是故障必需的（删任一项即变绿）。
3. **Phase 3 假设**：生成 **3-5 个可证伪假设**并排序，每个须声明预测（"若 X 是因，则改 Y 会让 bug 消失"）。单假设会锚定，禁止只提一个。
4. **Phase 4 插桩**：每次只改一个变量；优先用断点 / REPL，其次边界日志；所有临时日志加唯一前缀（如 `[DEBUG-a4f2]`），便于收尾时一次 grep 清除。性能回归走基线测量 + 二分，不走日志。
5. **Phase 5 修复 + 回归测试**：先写回归测试（仅当存在正确接缝——能复现真实 bug 模式的测试点），看它失败，再修，看它通过。若无正确接缝，记录此发现（架构阻碍了 bug 锁定）。
6. **Phase 6 收尾 + 复盘**：原始复现不再复现、回归测试通过、所有 `[DEBUG-...]` 已清除、临时探针已删、commit message 写明最终证实的假设。最后问：什么架构改动能预防此类 bug？

## 规则 7：测试驱动开发 —— 垂直切片，红绿重构

> 来源：[mattpocock/skills · tdd](https://github.com/mattpocock/skills/blob/main/skills/engineering/tdd/SKILL.md)

构建功能或修复 bug 时，采用**垂直切片**红绿重构，禁止水平切片（先写所有测试再写所有实现）。纯文档/配置/运维改动无测试接缝时，用可重复的验证命令替代；不得为了 TDD 造无真实调用方的接缝（规则 8）。

- **垂直切片**：一个测试 → 一个实现 → 重复。每个测试响应上一轮的发现，而非凭想象批量编写。
  ```
  错误（水平）：RED: test1..5  →  GREEN: impl1..5
  正确（垂直）：RED→GREEN: test1→impl1, test2→impl2, ...
  ```
- **测行为不测实现**：测试通过**公共接口**验证行为，不 mock 内部协作者、不测私有方法。好测试像规格说明（"用户可用有效购物车结账"），重构后不应失败。
- **最小代码**：只写让当前测试通过的最少代码，不预期未来测试、不加投机功能。
- **重构只在 GREEN 时**：全绿后才重构（提取重复、深化模块），每次重构后重跑测试。**禁止在 RED 时重构。**
- **每轮检查**：测试描述行为而非实现 ✓ ｜ 用公共接口 ✓ ｜ 能扛内部重构 ✓ ｜ 代码最小化 ✓ ｜ 无投机功能 ✓

## 规则 8：深模块设计 —— 小接口，大实现

> 来源：[mattpocock/skills · codebase-design](https://github.com/mattpocock/skills/blob/main/skills/engineering/codebase-design/SKILL.md)

设计或重构模块时，追求**深模块**：大量行为隐藏在小型接口之后，放在干净的接缝处，可通过该接口测试。

- **深 vs 浅**：深模块 = 小接口 + 大实现（如 `fs.readFile`）；浅模块 = 大接口 + 薄实现（仅透传，复杂度未减少）。设计接口时问：能减少方法数吗？能简化参数吗？能把更多复杂度藏进内部吗？
- **删除测试**：想象删掉该模块。若复杂度消失 → 它是透传（浅）；若复杂度在 N 个调用方重新出现 → 它在创造价值（深）。
- **接口即测试面**：调用方与测试穿越同一接缝。若想测到接口之外，模块形状大概率有问题。
- **接受依赖，不创建依赖**：依赖作为参数传入（可测试），而非在内部 `new`（难测试）。返回结果，不产生隐式副作用。
- **两个适配器才叫真接缝**：仅有一个适配器时接缝是假设的；只有当确实有第二种实现 / fake 时才引入接缝，不投机抽象。
- **设计建议不等于施工授权**：除非任务要求或总则 0.3 的 T3 计划批准，不主动重构、不提前抽象。

## 规则 9：Git 安全护栏 —— 禁止破坏性操作

> 来源：[mattpocock/skills · git-guardrails](https://github.com/mattpocock/skills/blob/main/skills/misc/git-guardrails-claude-code/SKILL.md)

本仓库不依赖 Claude Code hooks，以下为**行为级强约束**，任何 agent（含子 agent）禁止执行：

- `git push --force` / `git push --force-with-lease`（除非用户明确要求且不在 main/master）
- `git reset --hard`（丢失未提交改动）
- `git clean -f` / `git clean -fd`（删除未跟踪文件）
- `git branch -D`（强删未合并分支）
- `git checkout .` / `git restore .`（丢弃工作区改动）
- `git rebase -i` / `git add -i`（交互式操作，agent 无法可靠处理）
- 对 `main` / `master` 分支直接 force push

**例外**：上述禁令仅在用户明确指示且不涉及 `main`/`master` 时解除，并在执行前复述即将运行的命令请用户确认；对 `main`/`master` 的 force push 始终禁止。若当前环境无法获得交互确认（如审批策略禁止），不得执行例外操作，停止并报告。规则 3 的正常提交流程（`add` → `commit` → `push internal211 <分支>`）不受此限。

- `git commit --amend` / `git rebase` 仅限**未推送**的本地提交；已推送分支只追加新提交，禁止改写已发布历史。

## 规则 10：独立分析与工程判断

Agent 是完整、独立的思考个体。任务约束是输入之一，不是不可质疑的命令。

- **适用范围**：本规则用于审查用户任务约束与外部输入，不用于规避本文件除本规则外的其他规则或 Git 安全护栏；若冲突，先向用户说明并等待决策。
1. **综合分析**：将用户约束与当前代码、架构、运行时、成本与历史记录综合评估，先做理论与实践结合的判断，再执行；不机械照搬约束。
2. **研究拆解**：若某约束超出当前理解，先通过网络检索官方文档与可信资料拆解任务，不猜测。
3. **知识文件**：仅当调研结论会影响方案/代码/架构决策时，才必须整理为独立 Markdown 知识文件（文件名格式 `<实际主题>-<YYYY-MM-DD>.md`，开头写摘要，正文给出来源与关键结论）；单点事实核验可在回复中给出官方来源与结论，不强制新建文件。
4. **约束审查**：检查用户约束中的逻辑错误、逻辑跳跃、错误前提、信息缺失及与实际情况不符之处；不迎合用户，通过理论分析、数据挖掘和代码/架构事实获得独立判断。
5. **事实分离**：区分事实、推测与主观判断。涉及逻辑、数字、代码、架构的论断必须核实数据来源；外部事实优先引用官方文档或可核验来源；报告中标注 `事实 / 推测 / 判断`。
6. **直接异议**：不同意时直接指出，给出理由、风险、替代解释和可选方案；需要用户决策时提供真实选项。
7. **工程偏差**：显式补充用户输入中忽略的变量、成本、环境差异、事件偏差与长期运维成本。

## 规则 11：敏感资产本地化

- 入库文档（含 AGENTS.md、README、docs/）不得包含真实密钥、密码、订阅 token、SSH 凭据或私有服务账号。
- 真实凭据只存放于本地非仓库目录：Windows 为 `C:\Users\<user>\.axiom\axiom-secrets\`，Linux/macOS 为 `~/.axiom/axiom-secrets/`；仓库内只保留占位符（`${VAR}`）或 `.env.example` 模板。
- 已发现并移出的敏感文件：`docs/services.md`（含 SSH 密码、1Panel 凭据、机场订阅 token）→ `C:\Users\18336\.axiom\axiom-secrets\services.credentials`。禁止以任何形式恢复为仓库可追踪文件。
- 每次审计需扫描 git 跟踪文件与本次新增文件中的高熵密钥模式（如 `sk-*`、`AKIA*`、`ghp_*`、`-----BEGIN *PRIVATE KEY-----`），逐条确认命中项仅为测试夹具或占位符。
- **凭据泄露响应**：发现真实凭据进入仓库/工作区，立即停止传播并通知用户；轮换/吊销该凭据（移出仓库不等于失效）；清理工作区与必要的 git 历史；在 ops log 记录事件、影响面与补偿措施。凭据轮换需要权限时由用户执行，agent 不得擅自外传。
- 报告、ops log、commit message 不得回显密钥明文；只记录凭据名称、位置与处置状态。

## 规则 12：PCDA 施工闭环 —— 计划 → 执行 → 检查 → 处理

> 本仓库沿用既有术语 PCDA；阶段顺序为 Plan → Do → Check → Act。所有施工任务按此闭环推进，以规则 1 为红线，防止过度工程化与过度施工。PCDA 是施工方法，不替代总则 0.1 的优先级；冲突时按总则处理。

- **Plan（计划）**：按总则 0.3 判定任务级别，按总则 0.4 冻结任务契约（验收标准/改动清单/不做项/验证命令/风险回滚）。计划默认写在任务上下文或回复中，粒度与风险匹配——一行修复不写长篇计划，也不新建计划文件（任务本身要求或既有流程规定除外）。每项改动须能回答"删掉它，验收标准是否仍成立"；若能，则不写。
- **Do（执行）**：只执行已冻结清单内的最小改动。发现清单外问题：不影响验收标准的记入"不做/后续"；影响验收标准的暂停，回 Plan 更新清单与理由后再继续。禁止顺手重构、顺手清理、顺手新增"通用能力"。
- **Check（检查）**：运行 Plan 中的验证命令，按总则 0.5 核对完成证据，并核对：①每处改动都是验收必需；②无可删除的抽象/配置/依赖/文件；③无清单外改动。不满足则先删多余改动并复验。
- **Act（处理）**：Check 通过后固化有效做法（规则/测试/文档），按规则 5 留痕；返工与失败原因写入本轮记录；未解决项进入下一轮 Plan，不在本轮扩大范围。

**过度施工红线（命中即停，回 Plan 重评）**：

- 为单一调用点引入通用抽象/配置项/插件机制；
- 改动超出冻结清单；
- 新增依赖而现有依赖或标准库已能覆盖；
- 为"以后可能需要"写代码或测试；
- 同一提交混入多个不相关任务。

**完成判定**：Check 全绿 + Act 留痕 + 实际 diff 与冻结清单一致；三者缺一不得标记完成。

---

> 执行任何任务时，先对照以上全部规则规划流程，再动工。
