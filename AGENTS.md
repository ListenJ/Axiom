# AGENTS.md — Runtime 底层规则（强约束）

以下规则为本仓库所有任务的**强制约束**，任何 agent（含子 agent）执行任务时必须遵守。

## 规则 1：最小化施工，禁止过度施工

- 只做任务要求的最小改动；不顺手重构、不清理无关代码、不增加投机性的"通用能力"。
- 改动范围严格限定在任务涉及的文件与模块内。
- 新代码风格与周边代码保持一致。

## 规则 2：改代码前先备份 → 读全文 → 修改 → 验证 → 删备份

1. **备份**：修改任何文件前，先将其复制到 `.tmp/backups/`（保留相对路径结构，如 `.tmp/backups/src/foo/bar.ts`）。
2. **读全文**：修改前必须通读目标文件全文，禁止凭片段或记忆改动。
3. **修改**：按规则 1 做最小改动。
4. **验证**：运行覆盖该改动的检查（编译 / lint / 相关测试），确认通过。
5. **删除备份**：验证通过后，删除 `.tmp/backups/` 中对应备份。
6. **多子 agent 协同**：任务可拆分时，以最大可用子代理并行完成；每个子代理独立遵守本规则全部条款（各自备份、各自验证、各自记录）。

## 规则 3：所有修改提交到 git 仓库

- 目标仓库：`git@192.168.2.11`（remote 名：`internal211`，地址 `ssh://data@192.168.2.11/home/data/openclaw-fusion.git`，SSH 密钥已配置）。
- 每个任务的修改完成后：`git add <仅本任务相关文件>` → `git commit` → `git push internal211 <当前分支>`。
- **只暂存本任务相关文件**，不得把工作区中无关的未提交改动一并提交。
- 提交信息写清任务内容。

## 规则 4：删除 = 归档

- 任何"删除文件"操作实际执行为**归档**：
  1. 将文件移动到 `archive/` 下的合适位置（保持可识别的相对路径）。
  2. 在 `archive/ARCHIVE-LOG.md` 中追加一条归档记录：**归档时间、归档目的、原位置、归档位置**。
  3. 归档记录保留在仓库中，之后再将该文件从仓库工作区移除（`git rm`）并提交。
- 禁止未经归档记录直接删除任何文件。

## 规则 5：操作留痕 —— `docs/operations-log.md`

- 所有修改必须有可查询的痕迹：每次提交前，在 `docs/operations-log.md` 追加一条记录，**提交一次记录一次**。
- 每条记录包含：时间、任务描述、使用的工具（含子代理）、执行的操作（文件级）、验证结果、对应 commit hash（提交后补上）。
- 对应任务使用对应工具，并如实记录工具与操作。

---

> 执行任何任务时，先对照以上五条规则规划流程，再动工。
