# Axiom Runtime 全面审核报告（2026-08-11）

> 阶段：第一阶段审核（只读）已完结，第二阶段（协议研究 + 修复）进行中
> 分支：codex/frontend-aesthetic-repair
> 关联：docs/superpowers/specs/2026-08-11-frontend-aesthetic-repair.md、docs/plans/2026-08-11-frontend-aesthetic-repair.md、docs/PROTOCOL-COMPATIBILITY-2026-08-11.md

## 0. 审核方法与阵容

- 前端审美（代码级）：Darwin 子代理 + frontend-design / design-system / web-design-guidelines，21 页逐页审核，评分 7.4/10。
- 前端视觉（真实渲染）：Playwright 渲染 40 张截图（浅色/深色各 20 页）+ SenseNova 6.7 Flash-Lite 逐张视觉审核。
- 后端架构：Curie 子代理 + architecture / code-review / nodejs-backend-patterns / using-superpowers，60+ 文件通读，评分 3/5。
- 内核与配置：Locke 子代理 + 高熵扫描 + 配置链路审计 + 模板化设计。
- 视觉辅助：SenseNova 6.7 Flash-Lite（40 页）+ sensenova-u1-fast 生成 3 张原型图（深色/浅色动态背景、控制台壳层）。

## 1. 前端审核结论

### 1.1 P0（严重）

| # | 问题 | 证据 | 状态 |
|---|---|---|---|
| F1 | 默认浅色渲染 + 登录页/应用主题割裂 + 首屏 FOUC | frontend/src/state/useApp.ts:57-61（默认 system）；frontend/src/hooks/useTheme.ts:10-16；frontend/index.html:6 | 修复中 |
| F2 | Plugins 页无后端时渲染崩溃（marketplace HTML 字符串写入 state，Tabs badge 读 .length） | frontend/src/pages/Plugins.tsx（setMarketplace + badge） | 修复中 |
| F3 | 暗色下 Button danger/success hover 白字白底 | frontend/src/components/ui/Button.tsx:29,31；chat-panels.tsx:404 | 修复中 |
| F4 | BottomNav 激活指示条定位错位（父级无 relative） | frontend/src/components/layout/BottomNav.tsx:23-35 | 修复中 |
| F5 | 迷你聊天 nextId 每次渲染重建导致重复 key | frontend/src/components/rightbar/panels.tsx:736 | 修复中 |

### 1.2 P1（重要）

- 语义色全黑白决策方向成立（色盲友好），但：danger=accent=主文本同色；Eval 分数亮度反转（Eval.tsx:92-96 高分暗、低分亮）；浅色 --text-muted≈4.47:1、--info≈4.3:1 低于 AA。
- 移动端：消息操作按钮仅 hover 显示（chat-panels.tsx:512）；触摸目标大量 <44px；全站 100+ 处 text-2xs（10px）。
- 假骨架/永久加载态：Vault.tsx:122-124、Perf.tsx:105-112、Agents.tsx:82-90。
- 数据真实性：Router.tsx:63 全挂显示 ok；Code.tsx:133 状态卡固定绿色。
- 双套卡片语言（玻璃卡 vs 手写 border，30+ 处）；死代码 provider-hub-sections.tsx（482 行）；文档-代码漂移（强调色）。
- 40 页视觉审核共性：左栏辅助文字对比度低；错误态弱化（"工作区服务不可用"）；空态无引导；Tab 激活态弱；统计卡主次无差异；"+ 开启新对话"喧宾夺主；顶部文件/编辑/视图/帮助菜单风格不统一。

## 2. 后端架构结论（3/5）

- P0：双认知运行时未收敛（DRE 16.3k 行与主管线零数据流）；"已实现未接线"幽灵复杂度 ~2500 行（mathContext 6 模块、executeWithModeGuard、MultiDimensionLimiter、thompson-router 空实例、ConsensusEngine）；CI 必红（e2e/ 未入库但 CI 跑 E2E；缓存 key bun.lockb≠bun.lock）。
- P1：model-router 扇入 12 处直连；降级伪装成功（model-router.ts:383-388 返回 200）；route-auth.ts:60 非常量时间比较；执行模式未接线；data-pipeline.ts:864 每抓取 new VaultManager；错误形状三种并存；手写 Redis RESP2 无重连。
- 正面：安全纵深（fail-closed、SSRF 逐跳、HITL 闭环）有测试实证；深模块亮点多；as any 仅 3 处、process.env 直读归零、循环依赖 0。

## 3. 内核与配置结论

- 未发现真实密钥入库（符合 AGENTS.md 规则 11）；config/searxng/settings.yml:10 固定 secret_key 需模板化。
- P0：routes/health.ts:214 POST /config 无鉴权整文件覆写 git 跟踪 YAML；config-center sensitive 字段可明文写回；配置断链三件套（model-router.yaml 死配置、/models 死端、/chat 忽略 model 参数）。
- P1：ALLOWED_ENV_VARS 白名单致 ${ENV} 置空；yamlPath 数组下标错位；provider/baseURL 四表漂移（ofoxai/opencode/nim/minimax）；KIMICODE_API_KEY 拼写漂移。
- 模板化方案：.env（key/baseUrl/开关）+ config/model-router.yaml（模型/角色）双入口；新增 yaml-loader 深模块；POST /config 加鉴权；/models 接入 EXTENSIONS；/chat 支持 modelId。

## 4. 动态背景专项

- 现状：index.css:609-963 已有 8 层 CSS 动画（silk-aurora/sheen/ribs/fluid/swirl）+ 20+ backdrop-filter，有 reduced-motion 降级。问题：层数过多、大面积 blur 动画、无帧率自适应、无 visibilitychange 暂停、浅色光晕干扰阅读。
- 方向：8 层→3-4 层；动画只动 transform/opacity；预烘焙渐变替代实时 blur；WebGL 噪声着色器为终极形态；内容区毛玻璃隔离；浅色减光。
- 设计指导原型图与 Flash-Lite 规范：见可视化目录（proto-dark-bg / proto-light-bg / proto-dashboard）。

## 5. 最高杠杆行动清单

1. 默认主题修复（F1） 2. Plugins 崩溃（F2） 3. 语义色隐形（F3） 4. 配置断链 + POST /config 鉴权 5. 幽灵复杂度归档 6. model-router 单端口化 7. DRE 身份收敛 8. 动态背景治理 9. 移动端治理 10. 版本/文档单一事实源 + CI 修复。

## 6. 遗留

- 协议兼容矩阵：见 docs/PROTOCOL-COMPATIBILITY-2026-08-11.md（供应商文档研究后定稿）。
