---
type: audit-note
created: 2026-08-13
tags: [audit, code-review, security, configuration, frontend]
---

# 全工作区代码审计（2026-08-13）

> 方式：3 个并行只读审核子代理（后端核心 / 配置·安全 / 前端）+ 主代理高熵密钥扫描。未发现真实密钥入库（规则 11 通过）。

## 一、后端核心（评级：中 → 修复后良）

### P1（4 项，本次已修复 3 项）
| # | 问题 | 位置 | 状态 |
| --- | --- | --- | --- |
| 1 | vision.ts 路径穿越：markdown 媒体引用可读任意本地文件并外发智谱 API | src/knowledge/vision.ts resolveMediaPath | ✅ 已修复（白名单 + 拒绝绝对路径/`..` + 10MB 上限） |
| 2 | internal-agent/router 静默丢弃 maxTokens/timeout/signal（评测超时失效） | src/agents/internal-agent.ts、src/router/model-router.ts | ✅ 已修复（三层透传 + 回归测试 3 项） |
| 3 | skill_create 任意路径写文件（无守卫） | src/mcp/server/skill-tools.ts | ✅ 已修复（resolveSkillPath 白名单） |
| 4 | skill_run 将执行失败记为成功（污染质量/增益反馈） | src/mcp/server/skill-tools.ts | ✅ 已修复（检测 [Skill execution failed] 前缀） |

### P2 清单（15 项，待后续迭代）
runner 每任务全量重扫技能/clearSkillCache（P2-1）、spawnSync 阻塞事件循环（P2-2）、skill-gain 持久化未消毒（P2-3）、chat() 丢 maxTokens/signal（P2-4）、reload 丢运行时技能（P2-5）、execute 无超时（P2-6）、watch timer 未清理（P2-7）、bare skill 默认值不一致（P2-8）、CJK 分词失效（P2-9）、applySelfThought system 追加末尾（P2-10）、evolve 持久化重复+require 混用（P2-11）、CLI 参数校验缺失（P2-12）、confidence 规则不一致（P2-13）、vision 1305 死代码（P2-14）、报告/验证器弱项（P2-15）。

## 二、配置·安全（无 P0，4 项 P1）

| # | 问题 | 位置 | 状态 |
| --- | --- | --- | --- |
| 1 | config-center CONFIG_SCHEMA 模型数组下标与 axiom.yaml 顺序错位 | src/core/config-center.ts | ⏳ 待修（改为按 provider 名匹配） |
| 2 | resolveEnvVars 白名单使 axiom.yaml `${VAR}` 注入失效 | src/core/config-center.ts | ⏳ 待修（按 schema sensitive 字段放行） |
| 3 | .env.example 缺失 10 个业务 key 占位符 | .env.example | ✅ 已修复（KIMI/OPENCODE/MINIMAX/NIM/海外变体等补齐） |
| 4 | searxng 硬编码 secret_key + bind 0.0.0.0 | config/searxng/settings.yml | ✅ 已修复（bind 127.0.0.1 + 移除确定性密钥） |

### P2 要点：data/agent.db 明文 openrouter key（本地，建议配置 AXIOM_ENCRYPTION_KEY 迁移）、env.ts REQUIRED_ENV_VARS 命名错位（DATABASE_URL vs DATABASE_PATH）、.env.production.example 分裂、api-key-store PROVIDER_CONFIG 收敛不彻底（nim 重复/ofoxai 双域名/setup.ts opencode.gg 不一致）、visual-audit 默认值矛盾、README 控制字符损坏+版本统计矛盾、CI token 不足 16 位、package.json clean 删会话、operations-log 518KB。

## 三、前端（工程质量良好，但前后端契约严重漂移）

### P1（8 项，待修复）
模型选择器对 /chat/stream 无效（Chat.tsx:372）、编辑/重试发送过期上下文（Chat.tsx:409-440）、会话历史永不加载（history 返回形状不匹配）、迷你聊天请求体错误（panels.tsx 发 {message}）、KG stats 恒为"—"（Code.tsx vs /kg/stats）、深度研究结果不显示（search-panels）、插件/CodeGraph 写操作 403 confirmation 重试缺失、暗色主题白字白底徽标（chat-panels.tsx:404）。

### P2 要点：Router/Perf/Eval/OCR 统计字段错配、缓存命中率放大 100 倍、流式未卸载 abort、长会话 O(n) map、快捷键重挂、Tabs/combobox 键盘缺口、页面 effect 无清理、覆盖率 17% 核心页零测试、无 ESLint、一键提交推送风险、responseInterceptor 任意 token 字段。

## 四、修复记录

本次已提交安全修复：vision 路径白名单 + 10MB 上限、skill_create 路径约束、skill_run 失败反馈、searxng 密钥/绑定、.env.example 补齐。其余 P1/P2 记录于 docs/operations-log.md 后续迭代。
