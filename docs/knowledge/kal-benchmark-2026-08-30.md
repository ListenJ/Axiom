# KAL 检索基准：LIKE（现状） vs FTS5 trigram — 2026-08-30

> **摘要**：W5/W8 重立项前置门禁数据。合成 kg_nodes（CJK+英文混合）10k/50k/100k 共 3 档，对照组 A=现状 LIKE（照抄 `src/kal/knowledge-access-layer.ts` queryKG 的 LIKE SQL），B=FTS5 trigram（S2 基建同款虚拟表+触发器，脚本内内存库，非生产迁移）。3 类查询 × 每类 20 条，预热 1 次后计时 3 次取中位数，p50/p95 跨 20 查询。数字全部来自本次实跑（seed=42 可复现），总耗时 29363 ms。

> **结论行：FTS p95 中位增益 2.07x → 立项排期**

## 方法

- **语料**：内存 SQLite（`:memory:`，不入真实 `data/`），建表用 `src/kg/schema.ts` 的 `KG_SCHEMA_DDL`；name=中英混合标识符/中文串，description/semantic=中文串（2-4 词连写）与英文短语（2-4 词空格分隔）交错，importance 随机 0-1，其余列按 schema 填充。
- **A 组（现状 LIKE）**：`WHERE (name LIKE ? OR description LIKE ? OR semantic LIKE ?)`，整串 `%q%`，`ORDER BY importance DESC, id ASC LIMIT 10`——与 queryKG 生产 SQL 逐字一致（去 typeFilter 分支）。
- **B 组（FTS5 trigram）**：脚本内建 `kg_nodes_fts`（fts5 trigram 虚拟表 + AFTER INSERT/DELETE/UPDATE 同步触发器，镜像 `src/memory/sqlite-memory.ts` P1-S2 模式）。MATCH 规则镜像 `KAL.sanitizeFTS5(query, 3)`：词拆分、>=3 字符、`"词"*` OR 连接；**<3 字 CJK 短词走 LIKE 兜底腿**（镜像 queryVault 生产兜底模式），并集去重截 LIMIT 10。
- **查询集**（从实际写入语料的 token 采样，保证命中面真实）：
  - 精确词：5× 2 字 CJK（B 组走兜底腿）+ 5× >=3 字 CJK + 10× 英文整词；
  - 语序改写：10× 2 字 CJK + 英文双词乱序 + 10× >=3 字 CJK + 双英文三词乱序（整串 LIKE 语义天然难命中，FTS OR 语义可召回——召回差异即现状真实短板）；
  - 前缀：10× 英文词 3-5 字符前缀 + 10× 中文串 3 字前缀（均 >=3 字符，FTS 可覆盖）。
- **计时**：每查询预热 1 次 → 计时 3 次取中位数；p50/p95 为跨 20 条查询的 per-query 中位数分位数；召回均值 = LIMIT 10（生产默认）下实际返回行数。
- **环境**：Bun 1.3.14 / win32 / 12th Gen Intel(R) Core(TM) i5-12500H / 日期 2026-08-30。
- **门禁口径**：p95 增益 = LIKE p95 / FTS p95（FTS p95 下限 0.01ms 防除零，即增益上限封顶 100x 量级）；**中位增益 = 3 规模 × 3 类型共 9 格 p95 增益的中位数**；<2x → W5/W8 正式关闭（LIKE 现状保留），>=2x → 立项排期。

## 结果矩阵

### 10k 行

合成库构建（含 FTS 触发器同步索引写入）：693 ms

| 查询类型 | LIKE p50 (ms) | LIKE p95 (ms) | LIKE 召回均值 | FTS p50 (ms) | FTS p95 (ms) | FTS 召回均值 | p95 增益 | B 兜底腿查询数 |
|---|---|---|---|---|---|---|---|---|
| 精确词 | 2.81 | 3.08 | 10.0 | 0.49 | 2.92 | 10.0 | 1.06x | 5/20 |
| 语序改写 | 2.88 | 3.73 | 0.4 | 0.98 | 1.82 | 10.0 | 2.05x | 10/20 |
| 前缀 | 2.86 | 3.68 | 9.3 | 0.30 | 0.46 | 9.3 | 7.91x | 0/20 |

### 50k 行

合成库构建（含 FTS 触发器同步索引写入）：3783 ms

| 查询类型 | LIKE p50 (ms) | LIKE p95 (ms) | LIKE 召回均值 | FTS p50 (ms) | FTS p95 (ms) | FTS 召回均值 | p95 增益 | B 兜底腿查询数 |
|---|---|---|---|---|---|---|---|---|
| 精确词 | 15.7 | 17.3 | 10.0 | 3.15 | 15.6 | 10.0 | 1.11x | 5/20 |
| 语序改写 | 15.5 | 17.5 | 3.8 | 3.96 | 8.43 | 10.0 | 2.07x | 10/20 |
| 前缀 | 15.9 | 18.6 | 10.0 | 1.31 | 3.34 | 10.0 | 5.59x | 0/20 |

### 100k 行

合成库构建（含 FTS 触发器同步索引写入）：8691 ms

| 查询类型 | LIKE p50 (ms) | LIKE p95 (ms) | LIKE 召回均值 | FTS p50 (ms) | FTS p95 (ms) | FTS 召回均值 | p95 增益 | B 兜底腿查询数 |
|---|---|---|---|---|---|---|---|---|
| 精确词 | 31.9 | 34.8 | 10.0 | 7.44 | 32.1 | 10.0 | 1.08x | 5/20 |
| 语序改写 | 32.0 | 42.9 | 5.0 | 10.9 | 18.3 | 10.0 | 2.35x | 10/20 |
| 前缀 | 32.7 | 41.6 | 10.0 | 4.09 | 8.16 | 10.0 | 5.09x | 0/20 |

## 召回差异分析

- **语序改写**（最能体现现状短板）：LIKE 为整串子串匹配，乱序多词组合近乎零召回（均值 5.0/10）；FTS OR 语义可召回（均值 10.0/10，触顶即 10）。该差异是能力差异而非纯速度差异：即使速度增益不达标，召回鸿沟本身也是 W5/W8 的立项论据之一。
- **精确词**：2 字 CJK 词是中文查询主形态，trigram 最小 3 字符限制使其在 B 组仍走 LIKE 兜底腿（每类 5/20 条含短词），该类增益被兜底腿拉平——即 P1-S2 已知局限在 KG 侧同样成立。
- **前缀**：>=3 字符前缀两侧均可命中，差异主要来自扫描方式（全表三列 LIKE vs trigram 索引）。

## 结论

- **p95 中位增益（9 格中位数）：2.07x**
- **结论行：立项排期**

### 门禁解读

- 达到 2x 门槛 → W5（KG 索引优化）/W8 按本数据**立项排期**；落地形态建议：kg_nodes 侧 fts5 trigram 虚拟表 + 触发器（本脚本 B 组即原型），KAL queryKG 增加 MATCH 主腿 + 2 字 CJK LIKE 兜底腿（queryVault 同款模式），并接受语序改写召回能力提升这一附加收益。
- 局限：合成语料为随机组合，词频分布较真实语料均匀；内存库无页缓存竞争；单机单次运行，绝对值会随环境波动，结论以倍数（相对增益）为准。

